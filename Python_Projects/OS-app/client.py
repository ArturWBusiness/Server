import _thread as thread
import socket
import tkinter


algorithm:
if no config
    make it with defualt options
else
    read config.txt
    update the version
    set up a version

load graphics

set up defualt connection
